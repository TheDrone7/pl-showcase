```
/ The following program requests the user's name and greets the user accordingly. \

/ This is a comment and will be used to explain things \
/ The stdlib is fairly simple using keywords like `output` for output and `input` for input. \
/ Note that the comments start with a forward slash and need a backslash at the end to specify its ending. \

name = input('Please enter your name: ')
output('Hello, ' + name)
```