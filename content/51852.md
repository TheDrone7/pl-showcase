```
# This is a comment and will be used to explain stuff.
# The following program asks for your name and prints "Hello, name!"

# The print statement is being used to display output to the console.
# The var statement is used to declare a new variable, 'username' here.
# 'input' is a built-in function that requests user input from the console.

print("What is your name")
var username = input()

print("Hello, " + name + "!")
```