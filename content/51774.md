```
# This is a comment.

# out statement is used to display output and in statements request user input.
# in the second line, we are declaring a new variable called 'n' of the type 'string' and assigning the user entered value to it.

out("What is your name?")
string n = in()
out("Hello there!")
out(n)
```